<INSTRUCTIONS>
For this repository, after every completed code or project-file change:

1. Run `npm test`.
2. Run `node --check public/app.js public/markdown.js server.js`.
3. Review `git status -sb` and ensure only relevant source, test, or project files are included.
4. Commit the relevant changes with a concise Chinese commit message.
5. Push the commit to `origin/main`.

Do not commit Playwright artifacts, screenshots, logs, `node_modules`, or other ignored/generated files.
</INSTRUCTIONS>
